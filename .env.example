# Git Credentials for Private Repositories
# Copy this file to .env and fill in your credentials

# GitHub Personal Access Token (PAT)
# Generate one at: https://github.com/settings/tokens
# Required scopes: repo (for private repositories)
GIT_TOKEN=your_github_token_here

# Optional: Override default paths
# BASE_DIR=/root
# WORKDIR=/root/poktpool
# GIT_BRANCH=main

# ---- STORJ ----
# Storj bucket name
STORJ_BUCKET=blockchains

# Storj prefix/path within bucket
STORJ_PREFIX=postgres

# Storj Access Grant (required for uplink CLI)
# Get this from Storj web console or run 'uplink setup'
# This will be exported as UPLINK_ACCESS for the uplink CLI
STORJ_ACCESS_GRANT="your_access_grant_here"
